apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: victim-app
  namespace: victim
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: victim-app
  minReplicas: 1
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
